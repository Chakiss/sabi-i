'use client';

/**
 * PublicRoute - Component สำหรับหน้าที่ไม่ต้อง login
 * ใช้สำหรับแสดงเนื้อหาที่ทุกคนเข้าถึงได้
 */
const PublicRoute = ({ children }) => {
  return children;
};

export default PublicRoute;
